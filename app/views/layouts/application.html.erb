<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <title>Chacompao</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <style>
      <% if user_signed_in? && current_user.themepref == 'dark' %>
        body {
          /* background-color: #1c1e1f; */
          background-color: #121019;
          color: #fff;
        }
        a {
          color: #fff;
        }
        a:visited {
          color: #fff;
        }
        a:hover {
          color: #000;
          background-color: #fff;
        }
        .chords_menu, div.scplayer {
          /* background-color: #1c1e1f; */
          background-color: #121019;
        }
        .tdbutton a:hover {
            color: #fff;
        }
        .tdbutton:hover {
            /* background-color: #232729; */
            /* background-color: #251f33; */
            background-color: #191922;
        }
        p.notice, #notice {
          color: #8ae22b;
        }
        span.boldred, span.chords, p.alert, #alert {
          color: #ff688c;
          /* color: #f92472; */
          /* color: #fa1e4e; */
        }
        th, span.versiontitle, .scsub {
          color: #ddd;
        }
        /* ::-webkit-scrollbar-thumb {
          background-color: #ddd;
        } */
      <% end %>
    </style>
  </head>

  <body>
    <p class="notice"><%= sanitize notice %></p>
    <p class="alert"><%= sanitize alert %></p>
    <%= yield %>
  </body>
</html>
